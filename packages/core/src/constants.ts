/**
 * Default API base URL
 */
export const DEFAULT_API_BASE_URL = "https://api.bridgebase.dev";

/**
 * Gateway resolution endpoint path
 */
export const GATEWAY_RESOLVE_PATH = "/api/v1/gateway/resolve";

/**
 * Maximum JWT token size in bytes
 */
export const MAX_JWT_SIZE = 8192;

/**
 * JWT handshake timeout in milliseconds
 */
export const JWT_HANDSHAKE_TIMEOUT = 5000;

/**
 * Default proxy timeout in milliseconds
 */
export const DEFAULT_PROXY_TIMEOUT = 30000;

/**
 * Default proxy buffer size in bytes (64KB)
 */
export const DEFAULT_PROXY_BUFFER_SIZE = 65536;

/**
 * HTTP client timeout in milliseconds
 */
export const HTTP_CLIENT_TIMEOUT = 10000;
